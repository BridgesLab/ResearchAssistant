name: research-assistant
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - pip:
      - openai
      - bibtexparser
      - faiss-cpu
      - tiktoken
      - tqdm
      - streamlit
      - python-dotenv
